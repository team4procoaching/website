---
/**
 * Centered site footer with flat navigation links, social icons, and copyright.
 * Based on Tailwind UI "Simple centered" footer block.
 *
 * @example
 * ```astro
 * <Footer
 *   social={siteConfig.social}
 *   footerLinks={navigationConfig.footer}
 *   copyright={siteConfig.copyright}
 * />
 * ```
 */
import type { FooterLink } from '~/data/navigation';
import type { SocialLink } from '~/data/site';

type Props = {
  /** Social media links */
  social: readonly SocialLink[];
  /** Footer navigation links (flat list) */
  footerLinks: readonly FooterLink[];
  /** Copyright text */
  copyright: string;
};

const { social, footerLinks, copyright } = Astro.props;

// Filter out social links with placeholder href (e.g. '#' for upcoming YouTube channel)
const activeSocial = social.filter((s) => s.href !== '#');
---

<footer class="bg-surface-charcoal dark:bg-background-dark">
  <div class="mx-auto max-w-7xl overflow-hidden px-6 py-20 sm:py-24 lg:px-8">
    {/* Navigation links */}
    <nav aria-label="Footer" class="-mb-6 flex flex-wrap justify-center gap-x-12 gap-y-3 text-sm/6">
      {
        footerLinks.map((link) => (
          <a
            href={link.href}
            class="text-white/70 hover:text-white transition-colors dark:text-gray-400 dark:hover:text-white"
          >
            {link.label}
          </a>
        ))
      }
    </nav>

    {/* Social icons */}
    {
      activeSocial.length > 0 && (
        <div class="mt-16 flex justify-center gap-x-10">
          {activeSocial.map((item) => (
            <a
              href={item.href}
              target="_blank"
              rel="noopener noreferrer"
              class="text-white/70 hover:text-white transition-colors dark:text-gray-400 dark:hover:text-white"
            >
              <span class="sr-only">{item.name}</span>
              <svg
                viewBox="0 0 24 24"
                fill="currentColor"
                aria-hidden="true"
                class="size-6"
                {/* SAFETY: icon SVG path is statically defined in ~/data/site.ts via ~/data/icons.ts */}
                set:html={item.icon}
              />
            </a>
          ))}
        </div>
      )
    }

    {/* Copyright */}
    <p class="text-white/50 mt-10 text-center text-sm/6 dark:text-gray-400">
      {copyright}
    </p>
  </div>
</footer>
