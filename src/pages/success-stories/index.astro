---
/**
 * Success Stories overview page.
 * Showcases client transformations with a simple grid and testimonial quotes.
 *
 * Sections:
 * 1. Hero (HeroFullscreen) — Statement headline with background image
 * 2. Stories Grid — All success stories in a responsive grid
 * 3. Testimonial Grid — Masonry layout with short client quotes (social proof volume)
 * 4. CTA — Conversion prompt to contact
 */
import HeroFullscreen from '~/components/sections/HeroFullscreen.astro';
import SuccessStoryOverviewCard from '~/components/sections/successStories/SuccessStoryOverviewCard.astro';
import SuccessStoryReadMoreModal from '~/components/sections/successStories/SuccessStoryReadMoreModal.astro';
import TestimonialGrid from '~/components/sections/successStories/TestimonialGrid.astro';
import Cta from '~/components/ui/CTA.astro';
import Section from '~/components/ui/Section.astro';
import SectionHeader from '~/components/ui/SectionHeader.astro';
import { routes, successStoriesAnchors } from '~/data/routes';
import { successStories } from '~/data/successStories';
import { testimonials } from '~/data/testimonials';
import BaseLayout from '~/layouts/BaseLayout.astro';
import { remoteImage } from '~/types/components';

const heroBackground = {
  type: 'image' as const,
  src: remoteImage(
    'https://placehold.co/1920x1080/4a9199/f7eee5?text=Success+Stories+Hero',
    1920,
    1080,
  ),
  alt: 'Celebrating client transformations at Team 4 Pro',
};
---

<BaseLayout
  title="Success Stories"
  description="Real transformations from real women. See how our IFBB Pro coaches help clients achieve extraordinary results through competition prep, lifestyle coaching, and muscle building."
  headerVariant="on-dark"
>
  {/* 1. Hero */}
  <HeroFullscreen
    headline="Real Women. Real Results."
    primaryCta={{ label: 'Start Your Transformation', href: routes.contact }}
    secondaryCta={{ label: 'Explore Stories', href: successStoriesAnchors.stories }}
    background={heroBackground}
  >
    <p>
      Every transformation starts with a decision. See what happens when
      <strong>elite coaching</strong> meets <strong>unwavering commitment</strong>.
    </p>
  </HeroFullscreen>

  {/* 2. Stories Grid */}
  <Section
    background="muted"
    id="stories"
    ariaLabelledby="stories-heading"
    class="px-6 py-24 sm:py-32 lg:px-8"
  >
    <div class="mx-auto max-w-7xl">
      <div data-animate="fade-up">
        <SectionHeader headline="Success Stories" headingId="stories-heading">
          <p>
            From competition stages to everyday life — explore how our clients achieved their goals.
          </p>
        </SectionHeader>
      </div>

      {
        successStories.length > 0 && (
          <ul
            role="list"
            data-animate-stagger
            class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-8 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-3"
          >
            {successStories.map((story) => (
              <li data-animate="scale-up">
                <SuccessStoryOverviewCard story={story} />
              </li>
            ))}
          </ul>
        )
      }
    </div>
  </Section>

  {/* 3. Testimonial Quotes (Masonry Grid) */}
  <TestimonialGrid
    eyebrow="Testimonials"
    headline="What Our Clients Say"
    testimonials={testimonials}
  >
    <p>
      Don't just take our word for it — hear directly from the women who trusted us with their
      transformation.
    </p>
  </TestimonialGrid>

  {/* 4. CTA */}
  <Section background="muted" class="px-6 py-24 sm:py-32 lg:px-8">
    <div class="mx-auto max-w-4xl">
      <Cta
        headline="Ready to Write Your Success Story?"
        description="Whether you're preparing for competition, building muscle, or transforming your lifestyle — we're here to guide every step of your journey."
        primaryCta={{ label: 'Ready to Be Next?', href: routes.contact }}
        secondaryCta={{ label: 'Explore Services', href: routes.services }}
      />
    </div>
  </Section>

  {/* Success Story Read-More Modal — populated client-side from triggers above */}
  <SuccessStoryReadMoreModal stories={successStories} />
</BaseLayout>
